
| 阶段      | 要学内容                            | 为什么现在建立认知     | 后续用途            | 完成状态  |
| ------- | ------------------------------- | ------------- | --------------- | ----- |
| 基础知识    | 网络安全、AI 数学、AI 关键概念              | 建立共同语言、总得能读懂吧 | 阅读模型和应用安全资料、论文等 | 进行中（） |
| 模型安全    | 对抗样本、投毒、越狱、隐私泄露                 | 理解模型本身的风险     | 设计 LLM 安全用例     |       |
| 应用安全    | Prompt Injection、RAG、Agent、输出处理 | 理解真实系统攻击面     | 完成应用安全测试        |       |
| Web 基本功 | HTTP、认证、授权、XSS、SSRF、RCE、API     | 识别传统组件风险      | 分析 AI 应用后端和工具层  |       |
| 架构      | API、模型网关、向量库、Tool Calling       | 建立数据流和信任边界    | 设计实验系统          |       |
| 数据层     | 文档、Embedding、向量检索、权限            | 防止知识库投毒和越权检索  | 完成 RAG 实验       |       |
| 工具层     | 工具权限、参数校验、审批、日志                 | 防止 Agent 过度代理 | 完成 Agent 实验     |       |
| 输出层     | Markdown、HTML、代码、链接、敏感信息        | 防止下游执行或泄露     | 建立输出安全评测        |       |
| 实战      | 测试、评测、防护、报告                     | 把知识变成作品       | 完成五个作品          |       |


## AI 安全与传统 Web 安全的联系和区别：

### 目前认知：

传统Web安全倾向于寻找并利用网页或程序的隐藏信息或参数来完成RCE、文件上传、内网穿透、横向移动等行为并因此达到信息泄露或接管权限等危害行为

AI安全分是传统Web安全的扩展，AI安全不仅要保证服务能正常进行，还需要加上AI安全的独有内容，比如对用户的输入进行过滤、对大模型的输出进行限制，还有要设置大模型的底线防止大模型做出一些出格的事情

### GPT大人的整改和优化：

这段话抓住了一个核心事实：AI 应用安全与传统 Web 安全有继承关系，但又增加了模型特有的风险。不过，表述上有几处需要修正。

  主要问题：

  1. 把传统 Web 安全写成了攻击行为

     “寻找隐藏信息或参数来完成 RCE、文件上传……”描述的是渗透测试或攻击者行为，不是 Web 安全本身。Web 安全的目标应是保护机
     密性、完整性、可用性，并防止未授权访问、注入、越权、数据泄露等。

  2. “AI 安全是传统 Web 安全的扩展”不够准确

     对于带有网页、API、数据库和插件的 AI 应用，可以说 AI 应用安全是 Web/AppSec 与模型安全的交叉领域。但更广义的 AI 安全
     还涉及训练数据、模型供应链、隐私、模型窃取、对齐和内容安全，并不只是 Web 安全的下位扩展。

  3. “输入过滤、输出限制、设置底线”过于简化

     这些属于必要措施，但不是充分条件。输入过滤可能被提示注入、编码绕过或多轮对话绕过；输出限制也不能防止模型调用高权限
     工具或泄露检索到的数据。还需要身份认证、细粒度授权、工具隔离、数据访问控制、人工确认、审计和监控。

  4. 没有突出 AI 应用的关键差异

     传统程序通常按确定性逻辑执行，而大模型会理解自然语言、生成内容并自主决定是否调用工具。因此，用户输入既可能是普通数
     据，也可能成为“控制指令”；外部文档、网页和检索结果也可能污染模型上下文。这使得提示注入、越权调用、敏感信息泄露等风
     险更加突出。

  可以改成：

  > AI 应用安全与传统 Web 安全有很强的联系。AI 应用同样依赖 Web 页面、API、身份认证、数据库和文件系统，因此仍然需要防范
  > 注入、越权、SSRF、文件上传、数据泄露等传统 Web 风险。
  >
  > 但 AI 应用还引入了模型特有的风险：自然语言输入可能影响模型行为，外部文档可能通过提示注入操纵模型，模型输出可能不可
  > 靠，模型还可能调用数据库、代码执行器或其他高权限工具。因此，AI 安全不能只依靠输入过滤和输出限制，还必须结合模型防
  > 护、数据隔离、最小权限、工具调用控制、人工确认以及全过程审计。
  >
  > 简单来说，传统 Web 安全主要保护程序、接口和基础设施；AI 应用安全则是在此基础上，进一步保护模型的行为、上下文、数据访
  > 问和工具使用权限。


## 为什么AI 应用安全需要同时懂模型、Web、数据和权限

AI 应用安全本质上是“模型能力”与传统软件系统结合后的复合安全问题，因此研究AI 应用安全需要同时学习模型、Web、数据、权限等知识。

  - 模型：模型可能被提示注入、越权指令、上下文污染或诱导泄露敏感信息。它还可能产生不可靠输出，影响后续业务决策。
    
  - Web：模型通常通过网页、API、插件和工具与外部世界交互。SQL 注入、SSRF、XSS、请求伪造等传统 Web 漏洞，可能被模型放大或自动利用。

  - 数据：模型依赖训练数据、知识库、向量库、会话记录和用户上传文件。数据投毒、越权检索、隐私泄露和跨租户数据混淆，都会直接变成 AI 风险。

  - 权限：模型一旦能调用邮件、数据库、文件系统或支付接口，就不只是“聊天工具”。如果身份、授权和工具权限设计不严，攻击者可以借模型执行本不该执行的操作。

  真正危险的地方在于它们会串联成攻击链：

  > 恶意文档注入指令 → 模型相信指令 → 调用高权限工具 → 读取内部数据 → 通过 Web 接口泄露。

  所以 AI 应用安全需要同时理解：

  1. 模型如何理解和执行指令；
  2. Web 请求和工具调用如何工作；
  3. 数据如何存储、检索和流转；
  4. 用户与模型分别拥有哪些权限。

  可以把模型视为一个“不完全可信、但行动能力很强的代理”。安全设计应采用最小权限、输入与输出隔离、数据访问控制、工具调用确认、审计监控和纵深防御，而不能只依赖提示词或模型本身“听话”。